services:
  # PHPウェブサービス
  - type: web
    name: fusion-device-manager
    env: docker
    region: singapore  # またはお好みのリージョン
    plan: free  # または starter / standard
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    envVars:
      - key: DB_TYPE
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: fusion-postgres
          property: host
      - key: DB_PORT
        fromDatabase:
          name: fusion-postgres
          property: port
      - key: DB_NAME
        fromDatabase:
          name: fusion-postgres
          property: database
      - key: DB_USER
        fromDatabase:
          name: fusion-postgres
          property: user
      - key: DB_PASS
        fromDatabase:
          name: fusion-postgres
          property: password
    # 注意: ディスクは有料プラン（Starter以上）でのみ利用可能
    # 無料プランの場合は以下をコメントアウト
    # disk:
    #   name: fusion-uploads
    #   mountPath: /var/www/html/uploads
    #   sizeGB: 1

  # PostgreSQLデータベース
databases:
  - name: fusion-postgres
    databaseName: device_management
    user: admin
    region: singapore  # Webサービスと同じリージョン
    plan: free  # 無料プラン（90日間アクティブ）
