services:
  # PHPウェブサービス
  - type: web
    name: device-manager
    env: docker
    region: singapore  # またはお好みのリージョン
    plan: free  # または starter / standard
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    envVars:
      - key: DB_TYPE
        value: mysql
      - key: DB_HOST
        value: sql.freedb.tech
      - key: DB_PORT
        value: 3306
      - key: DB_NAME
        value: freedb_mysqltestdb
      - key: DB_USER
        value: freedb_neshotam27
      - key: DB_PASS
        value: QAETdc3xq$g$NSV
    # 注意: ディスクは有料プラン（Starter以上）でのみ利用可能
    # 無料プランの場合は以下をコメントアウト
    # disk:
    #   name: fusion-uploads
    #   mountPath: /var/www/html/uploads
    #   sizeGB: 1
